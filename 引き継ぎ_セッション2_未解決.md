# セッション引き継ぎ — 動画の音ズレ・画質問題（未解決）

## 作成日: 2026年2月20日
## 前回資料: 引き継ぎ_動画機能_ClaudeCode向け.md（必ず先に読むこと）

---

## 1. 問題（未解決）

動画を再生すると：
1. **音ズレ** — 映像と音声がずれる・速度が遅い
2. **元動画と違う** — 変換後の動画が元の画質・見た目と異なる

この2つが同時に起きている。

---

## 2. 原因

- スマホ動画は**VFR（可変フレームレート）**で保存される
- HTML5 videoタグはCFR（固定フレームレート）前提 → 音ズレが起きる
- 音ズレを直すにはffmpegで**再エンコード（CFR化）**が必要
- しかし再エンコードすると画質が変わる

この2つは本質的にトレードオフの関係にある。

---

## 3. 現在のコード状態（main ブランチ）

### フロントエンド（deploy/index.html）— Netlify自動デプロイ
- **2281行目: `video.src = photo.photoURL`** — 元動画をそのまま再生している
- convertedURL（変換済み動画）を無視している
- 「変換中」バッジは削除済み
- `checkPendingVideoConversions()` 関数は存在するが呼ばれていない

### Cloud Functions（functions/index.js）— 別途 firebase deploy が必要
- **`-c:v copy -c:a copy`** — 映像・音声をコピー（再エンコードなし）
- これはコンテナの再パッケージのみで、VFRのCFR化はしない
- つまり**音ズレは直らない設定**になっている

### 要するに
- フロントエンド → 元動画を再生（音ズレあり）
- Cloud Function → コピーするだけで変換していない（音ズレ直らない）
- **両方とも音ズレを解決できない状態**

---

## 4. 前セッションでやったこと（このセッションの失敗記録）

| やったこと | 結果 |
|---|---|
| ffmpegの再エンコード(CRF23)→コピー(-c:v copy)に変更 | 音ズレ対策を壊した |
| フロントでconvertedURL→photoURLに変更 | 変換動画を無視するようにした |
| 「変換中」バッジを削除 | UIの問題は消えたが本質ではない |
| Firestoreクエリのインデックス修正 | 関係なかった |
| PRを5個作成・マージ | コードは入ったが問題は未解決 |

**全体として、音ズレ対策（ffmpeg再エンコード）を「画質劣化の原因」と誤診し、その対策を全て取り除いた。結果、何も解決していない。**

---

## 5. 本来やるべきこと

### Cloud Functions（functions/index.js）
ffmpegの設定を `-c:v copy` から再エンコードに戻す。ただし画質を最大限保つ：

```javascript
ffmpeg(tempInput)
  .videoCodec("libx264")
  .outputOptions([
    "-crf 18",              // 18 = ほぼ視覚的にロスレス（前回は23で画質劣化が目立った）
    "-preset medium",       // 品質とエンコード速度のバランス
    "-r 30",                // CFR 30fps（VFR→CFR変換で音ズレ解消）
    "-profile:v high",      // high profile（baselineより高画質）
    "-pix_fmt yuv420p",     // ブラウザ互換性
    "-movflags +faststart", // ストリーミング最適化
  ])
  .audioCodec("aac")
  .audioBitrate("192k")    // 前回128kだった
```

**重要: Cloud Functionsはfirebase deployしないと反映されない。**
```bash
cd functions && npm install && cd .. && firebase deploy --only functions
```

### フロントエンド（deploy/index.html）
`video.src` を変換済み動画に戻す：
```javascript
// 2281行目を変更
video.src = photo.convertedURL || photo.photoURL;
```

変換中の表示も復活させるべき。

---

## 6. デプロイ構成

| 対象 | デプロイ方法 | 自動/手動 |
|---|---|---|
| フロントエンド（deploy/） | Netlify | GitHub mainブランチから自動デプロイ |
| Cloud Functions（functions/） | Firebase | `firebase deploy --only functions` が必要 |

- **本番URL**: https://starlit-jelly-84dacb.netlify.app/
- **Firebase Project**: kids-growth-700b0（Blazeプラン）

---

## 7. テストデータ

- Firestoreに動画のテスト投稿が1件ある（2026/2/11、コメント「俺のおもちゃ」、2MB/8秒）
- この動画で音ズレ・画質を確認できる

---

## 8. この環境の制約

- mainブランチに直接pushできない（claude/で始まるブランチのみ）
- firebase CLI の認証がない（Cloud Functionsをデプロイできない）
- 本番サイトに外部からアクセスできない（確認不可）
- **つまり、Cloud Functionsの変更はコードを書いてpushすることはできるが、デプロイと動作確認はユーザー自身が行う必要がある**

---

## 9. 前セッションの反省（次のセッションへ）

1. **引き継ぎ資料を最初に読め** — 前回は読まずに作業を始めて的外れな修正を重ねた
2. **問題の原因を正確に理解してから修正しろ** — 「画質劣化」と「音ズレ」を混同した
3. **デプロイ構成を把握しろ** — Netlifyは自動、Cloud Functionsは手動という違いを把握していなかった
4. **自分で確認できないなら、確認方法をユーザーに具体的に伝えろ**
5. **同じ指摘を繰り返させるな** — ユーザーの指摘を忘れて同じミスを繰り返した
